# Master MLB Blender Machine V52 — True Multi Upload

This version hard-codes Streamlit's uploader as:

accept_multiple_files=True

Use:
- Upload all 4 split CSV files together.
- The feeder merges them into one slate.
- Same six tabs only.

Run:
streamlit run app.py
