# THE BLENDER v31 Machine Parts

This is the first real machine-folder build.

Parts:
- parser/ = PDF, CSV, XLSX readers
- engine/ = gates, scoring, machine run, core builder notes
- audit/ = feeder lock / no fake locks
- ui/ = style, shell, wheel, cards, tickets
- data/ = team colors and aliases
- exports/ = CSV output pieces
- legacy/ = v30 working backups

Core law stays locked: one owner per game.
