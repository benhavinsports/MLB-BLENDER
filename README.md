# MLB Blender Machine v14 — STRICT OPTIMIZED 18-GATE ENGINE

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```

Uses only the official MLB Stats API:
- /schedule?sportId=1
- /game/{gamePk}/boxscore
