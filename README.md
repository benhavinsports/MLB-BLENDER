# MLB Blender v54 Section-Bound Reader

4 files only.

Fix:
- PDF hitter rows are bound to the exact team/pitcher section span they appear under.
- Multiple headers on one page are split into separate spans.
- Carryover only applies before the next detected header.
- Unknown games are page-bucketed instead of collapsed together.
