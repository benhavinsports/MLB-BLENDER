# MLB Home Run Blender — Auto Gate Machine

## What this version does
- Auto-pull attempt from Star Tool URL
- Upload PDF / CSV / XLSX fallback
- Parses Star Tool-style rows
- Runs strict gate log
- Dead players cannot return

## Important
Auto-pull works only if the source page exposes data without login or JavaScript blocking.
If URL pull shows no rows, use PDF upload.
